import 'package:flutter/material.dart';

class PairScreen extends StatelessWidget {
  const PairScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Pairing Screen'));
  }
}
