import 'package:flutter/material.dart';

class PastAppointments extends StatelessWidget {
  const PastAppointments({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('past', style: TextStyle(fontSize: 24)),
    );
  }
}